import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Som dos Animais',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
